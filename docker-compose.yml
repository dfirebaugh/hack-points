version: '2'
services:
    web:
        build: .
        ports:
            - ${PORT}:${PORT}
        links:
            - hack-points-db
    hack-points-db:
        image: mongo
        volumes:
            - /data/mongodb/db:/data/db
        ports:
            - '27017:27017'
